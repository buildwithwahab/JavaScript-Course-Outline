// ==============================
// JAVASCRIPT EVENT LISTENERS & DOM MANIPULATION – CORE OUTLINE
// ==============================

// 📘 1. What is the DOM?
// - DOM = Document Object Model
// - Represents HTML as objects in a tree structure

// 📘 2. Selecting Elements
// - getElementById()
// - querySelector() / querySelectorAll()
// - getElementsByClassName(), getElementsByTagName()

// 📘 3. Changing Content
// - innerHTML, innerText, textContent
// - value (for form inputs)

// 📘 4. Changing Styles and Attributes
// - style.property = "value"
// - classList.add(), remove(), toggle()
// - setAttribute(), getAttribute()

// 📘 5. Creating and Removing Elements
// - createElement()
// - appendChild(), prepend(), insertBefore()
// - removeChild(), remove()

// 📘 6. Event Listeners
// - addEventListener(event, callback)
// - Common events: click, submit, input, change, keydown
// - Removing event listeners

// 📘 7. The Event Object
// - Accessing event.target, event.type, etc.
// - Preventing default behavior with event.preventDefault()

// 📘 8. Form Handling with Events
// - Handling user input
// - Validating before submit
// - Accessing form values

// 📘 9. DOM Traversal
// - parentElement, children, nextElementSibling, previousElementSibling

// 📘 10. Best Practices
// - Add event listeners after DOM is loaded
// - Use `const` and `let` instead of `var`
// - Keep logic outside HTML (no inline handlers)
